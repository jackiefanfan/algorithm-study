class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        a = 0
        b = 0
        l = []
        h = ""
        for n in range(len(num1)): 
            
            a = a + (ord(num1[n]) - ord('0')) * (10**(len(num1)-1-n))
        for m in range(len(num2)):
            
            b = b + (ord(num2[m]) - ord('0')) * (10**(len(num2)-1-m))
        
        return str(a*b)
